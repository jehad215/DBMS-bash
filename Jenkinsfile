pipeline {
    agent any

    stages {
        stage('list') {
            steps {
                git 'https://github.com/jehad215/DBMS-bash.git'
                sh 'ls'
            }
        }
    }
}
